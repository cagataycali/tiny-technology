{
  "name": "chatgpt-plugin-tiny",
  "version": "0.0.1",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "@types/node": "^20.4.5",
    "typescript": "^5.1.3",
    "wrangler": "^4.0.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy:default": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "deploy": "npm run deploy:default && npm run deploy:production"
  },
  "dependencies": {
    "@cloudflare/itty-router-openapi": "^1.1.1",
    "openai": "^4.11.0",
    "slugify": "^1.6.6"
  }
}
