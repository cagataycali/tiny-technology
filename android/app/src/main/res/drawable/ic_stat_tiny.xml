<?xml version="1.0" encoding="utf-8"?>
<!--
  Status-bar small icon: the tiny "growing node" mark as a WHITE-ON-TRANSPARENT
  silhouette. Android draws notification small icons using only the drawable's
  alpha channel and tints the result, so the launcher foreground's dark #020604
  fill disc renders as a featureless white blob and mipmap/ic_launcher renders as
  a plain white square. This icon carries NO fill — just the ring + sprout as
  strokes, widened from the launcher's 3 to 8 (of a 108 viewport) so the outline
  survives the small-icon downscale. Matches ic_shortcut_voice's white-glyph rule.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="108"
    android:viewportHeight="108"
    android:tint="#FFFFFFFF">
    <path
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="8"
        android:fillColor="#00000000"
        android:pathData="M54,54m-20,0a20,20 0,1 1,40 0a20,20 0,1 1,-40 0" />
    <path
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="8"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000"
        android:pathData="M54,34 L54,24 M54,24 C54,24 47,22 46,15 C53,15 54,24 54,24 C54,24 55,17 62,16 C61,23 54,24 54,24" />
</vector>
